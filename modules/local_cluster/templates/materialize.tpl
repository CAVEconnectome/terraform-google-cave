# Generated values for Materialization Engine
materialize:
  schedules: []
  redis:
    host: "${redis_host}"
limiter:
  redis:
    host: "${redis_host}"
cloudsql:
  projectId: "${project_id}"
  instance: "${sql_instance_name}"
